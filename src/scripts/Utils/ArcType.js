const ArcType = {
  full() {
    return Math.PI * 2;
  },

  half() {
    return Math.PI;
  },

  third() {
    return Math.PI * 2 / 3;
  },

  quarter() {
    return Math.PI / 2;
  },

  eighth() {
    return Math.PI / 2 / 2;
  }
}

export default ArcType;